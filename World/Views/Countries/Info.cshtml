@using World.Models;

<h1>Countries List</h1>

<ol>
@if (Model.Count == 0)
{
  <p>You have no countries in your list!</p>
}
@if(Model.Count != 0)
{
  @foreach (Country country in Model)
  {
    <li>@country.GetId(), @country.GetCountryCode()</a></li>
  }
}
</ol>
